# Explicit setup pointer only for hard dependencies

Engineering skills should point to `/setup-matt-pocock-skills` only when they have a hard dependency on repo-local issue tracker, triage label, or domain-doc configuration. This keeps setup prompts visible where missing context would break behavior, without turning every skill into setup documentation.

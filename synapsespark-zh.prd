# SynapseSpark（突触火花）— 产品需求文档

> 从现有项目代码生成 | 2026-06-04
> 注册类别：product | 技术栈：Next.js + React 19 + Tailwind v4 + Motion

---

## 1. 产品定义

### 1.1 这是什么

SynapseSpark 是一个**突触式词语联想探索工具**。用户输入一个起始词；引擎向外辐射五个语义相关的词语。点击其中一个，它即成为新的中心，再产生五个新的联想，而先前的关联网络则淡化为探索轨迹。每一个访问过的节点都会作为可点击足迹保留在画布上，支持随时回溯和重新分支。

### 1.2 为什么存在

创意思维是非线性的。思维导图强加层级，聊天界面强加线性。SynapseSpark 模拟的是联想思维实际上的漂移方式——一个词引爆五个，五个引爆二十五个，而在不断分支中，某个意想不到的连接便会浮现。

### 1.3 目标用户

处于心流状态的创意专业人士：设计师、写作者、产品经理、研究者。他们在桌前，专注地解决某个特定创意问题。这个工具必须像精密仪器一样，而非玩具。

---

## 2. 当前实现

### 2.1 文件与界面

| 文件 | 用途 | 状态 |
|------|------|------|
| `index.html` | 品牌落地页，包含 Hero、Bento 功能网格、使用步骤、统计数据 + CTA | 已完成 |
| `explore.html` | 全屏探索画布（核心产品） | 进行中 |
| `src/app/page.tsx` | Next.js 入口 — 目前仅渲染一个垂直菜单组件 | 骨架搭建 |
| `src/app/layout.tsx` | Next.js 根布局及元数据 | 骨架搭建 |
| `src/app/globals.css` | 仅导入 Tailwind v4 | 骨架搭建 |
| `src/components/ui/menu-vertical.tsx` | 可复用的交错菜单 React 组件 | 已完成 |

### 2.2 落地页（`index.html`）

**各板块：**
1. **导航栏** — 固定磨砂玻璃条，含 Logo（SVG 神经图图标）、交错菜单开关、"立即试用"CTA
2. **Hero** — 不对称双栏：磁性排版标题 + 带脉冲动画的抽象神经网络 SVG 星座图
3. **功能 Bento 网格** — 12 列网格：主卡占 7 列（五向扩散演示）、两张 5 列卡（轨迹、书签+笔记）、三张 3 列卡（精选词库、零学习曲线、免费）
4. **使用步骤** — 密集 2 列网格，4 个编号步骤（01-04）
5. **引擎规格** — 水平滚动轨道，4 张规格卡（<50ms 响应、无限深度、375px 最小宽度、MIT 许可）
6. **统计数据 + CTA** — 4 项统计（2000+ 词汇、5 条分支、零学习时间、100% 免费）+ 双 CTA 按钮
7. **页脚** — 极简 Logo + 标语

**值得注意的技术细节：**
- GSAP 3.12 + ScrollTrigger 用于滚动驱动的揭示动画和统计数字滚动
- Hero 标题的磁性排版（字符级光标排斥效果）
- CTA 标题的弹性渐入交错文字
- 交错菜单系统：双层预叠加彩色面板（紫色渐变）+ 面板滑入（含编号项）+ 开关切换时的文字翻转动画
- 完整 i18n 框架：繁体中文、简体中文、英文 — 通过 `data-i18n` 属性动态替换内容
- 所有按钮均有波纹点击效果
- 响应式断点：1024px、768px、640px、480px + 横屏手机 + 粗指针
- 全局遵循 `prefers-reduced-motion`

### 2.3 探索画布（`explore.html`）

核心产品界面。全屏深色画布，包含：
- 带径向遮罩的点阵背景图案
- 起始词输入区
- 节点与连线渲染画布
- 带笔记附件的书签系统
- 已访问节点的轨迹保留

---

## 3. 设计系统

### 3.1 设计令牌

```css
/* 背景 */
--bg: #0D1117;           /* 主画布/背景 */
--bg-secondary: #161B22;  /* 卡片、面板表面 */
--bg-tertiary: #1C2333;   /* 按钮、悬浮悬停 */
--bg-elevated: #1A1E2E;   /* 提示条、对话框 */

/* 前景 */
--fg: #E6EDF3;            /* 主文字 */
--fg-secondary: #8B949E;  /* 次要文字、正文 */
--fg-tertiary: #484F58;   /* 说明文字、禁用态 */

/* 强调色 */
--accent: #7C3AED;        /* 主强调色（紫罗兰） */
--accent-hover: #8B5CF6;  /* 悬停态 */
--accent-soft: rgba(124,58,237,0.15); /* 微妙强调色背景 */
--accent-2: #DB2777;      /* 次要强调色（粉色） */
--accent-2-soft: rgba(219,39,119,0.12);
--accent-3: #3B82F6;      /* 第三强调色（蓝色） */
--accent-3-soft: rgba(59,130,246,0.1);

/* 语义色 */
--green: #6A9955;
--border: #30363D;
--border-strong: #484F58;

/* 玻璃效果 */
--glass-bg: rgba(13,17,23,0.88);
--glass-border: rgba(255,255,255,0.06);
```

### 3.2 字体排版

| 令牌 | 字体栈 | 用途 |
|------|--------|------|
| `--font-display` | Manrope, Inter, Segoe UI, system-ui | 标题、导航、按钮 |
| `--font-body` | Inter, Segoe UI, PingFang SC, system-ui | 正文、界面文案 |
| `--font-mono` | Cascadia Code, JetBrains Mono, Fira Code | 数据、代码 |
| `--font-decorative` | Georgia, Times New Roman, STSong | Logo 文字标 |

**字号层级：** Hero `clamp(3.2rem, 7vw, 5.8rem)` 字重 800 · 章节标题 `clamp(2rem, 4vw, 3.2rem)` 字重 700 · 正文 `clamp(0.95rem, 1.4vw, 1.1rem)` · 说明文字 13px · 眉标 12px 大写 字重 700

### 3.3 圆角与间距

| 令牌 | 值 |
|------|-----|
| `--radius-sm` | 10px |
| `--radius-md` | 16px |
| `--radius-lg` | 24px |
| `--radius-xl` | 32px |
| 按钮 | 完全胶囊形（`9999px`） |
| Bento 间距 | 18px |
| 章节内边距 | 垂直 60-120px |

### 3.4 动效

| 属性 | 值 |
|------|-----|
| 缓出曲线 | `cubic-bezier(0.16, 1, 0.3, 1)` |
| 弹性曲线 | `cubic-bezier(0.22, 0.61, 0.36, 1)` |
| 按钮悬停 | `translateY(-1.5px) scale(1.025)` |
| 按钮按下 | `scale(0.955)` 持续时间 100ms |
| 波纹扩散 | 520ms 缓出 |
| 滚动揭示 | `y: 32 → 0` 持续时间 700ms power3.out |
| 逐项交错 | 60ms（弹性字符）、80-100ms（菜单项） |
| 面板关闭（快速）| 250ms power3.in |

### 3.5 组件

| 组件 | 位置 | 备注 |
|------|------|------|
| 导航栏 | `index.html`、`explore.html` | 固定定位，磨砂玻璃，56px 高度 |
| 按钮（主/次/幽灵/浅色）| `index.html` CSS | 胶囊形，波纹反馈，3 态按压系统 |
| Bento 卡片 | `index.html` CSS | 12 列网格，交互式变体带悬停抬升 |
| 功能单元格 | `index.html` CSS | 2 列密集网格，1px 间隙边框 |
| 水平滚动卡片 | `index.html` CSS | 340px 固定宽度，交错顶部边框颜色 |
| 交错菜单 | `index.html` JS + `menu-vertical.tsx` | 双层预叠加 + 面板，文字翻转开关，编号项 |
| 标签/胶囊 | `index.html` CSS | 圆角胶囊，3 种颜色变体 |
| 提示条 | `index.html` CSS + JS | 底部居中胶囊，弹性入场 |
| 图标框 | `index.html` CSS | 44px 圆角方形，3 种颜色变体 |
| 步骤编号 | `index.html` CSS | 64px 展示字重数字 |
| 统计数值 | `index.html` CSS | 大号数字，GSAP 计数动画 |

---

## 4. 交互模式

### 4.1 核心循环

```
[输入词语] → [5 个联想辐射] → [用户点击一个]
→ [选中词成为中心] → [5 个新联想辐射]
→ [先前网络淡化为轨迹] → [重复]
```

### 4.2 画布交互

| 操作 | 手势 | 反馈 |
|------|------|------|
| 开始探索 | 输入词语 + 回车 | 5 个节点从中心向外动画展开 |
| 追踪分支 | 点击/轻触候选节点 | 中心移动，新节点辐射展开（300ms） |
| 回溯轨迹 | 点击/轻触已淡化的足迹节点 | 节点重新激活，5 个新方向展开 |
| 平移画布 | 拖拽/滚动 | 跟随拖拽，惯性衰减 |
| 缩放 | 滚动/捏合 | 0.3x–3x 范围 |
| 重置视图 | 双击空白区域 | 居中至当前活动节点 |
| 书签 | 点击书签图标 | 节点加入收藏侧边栏 |
| 添加笔记 | 点击已收藏节点的笔记图标 | 显示内联文本输入框 |

### 4.3 状态处理

| 状态 | 处理方式 |
|------|----------|
| 空/首次访问 | 居中输入框，提示："输入一个词开始探索" |
| 加载中（词语获取）| 中心节点微微脉动，无加载动画 |
| 词库中找不到该词 | 语义相似度回退匹配，静默处理 |
| 错误 | 提示条通知，非阻塞式 |
| 画布为空（重置后）| 返回初始输入状态 |

---

## 5. 国际化

三种语言，存储在 `index.html` 的 JS 对象中：

| 代码 | 语言 | 覆盖范围 |
|------|------|----------|
| `zh-Hant` | 繁体中文 | 完整（所有界面字符串） |
| `zh-Hans` | 简体中文 | 完整 |
| `en` | 英文 | 完整 |

机制：DOM 元素上的 `[data-i18n]` 属性，运行时通过 `applyI18n(lang)` 替换。语言选择通过交错菜单底部的 `[data-lang]` 按钮状态持久化。

---

## 6. 技术架构

### 6.1 技术栈

| 层级 | 技术 | 理由 |
|------|------|------|
| 框架 | Next.js 16（React 19）| SEO + 性能 + 为未来后端提供 API 路由 |
| 样式 | Tailwind CSS v4 | 原子化优先，设计令牌以 CSS 变量形式存在 |
| 动画（React）| Motion（Framer Motion）v12 | 声明式，布局感知 |
| 动画（落地页）| GSAP 3.12 + ScrollTrigger | 复杂时间线、滚动驱动、统计计数 |
| 图标 | Lucide React | 统一的 SVG 图标集 |
| 打包器 | Webpack（通过 `--webpack` 标志）| Turbopack 兼容性变通方案 |
| 测试 | Playwright | E2E 与视觉回归测试 |
| 包管理器 | npm | 锁定文件已纳入版本管理 |

### 6.2 词库引擎（MVP）

- 离线优先：静态 JSON 词库与应用打包在一起
- 2000+ 精选词汇，每个词含 5–10 个语义关联
- 词汇查找：哈希映射，O(1)
- 未知词汇回退：针对已知条目进行语义相似度匹配
- 响应目标：每次扩散 <50ms

### 6.3 渲染策略

当前实现使用基于 DOM 的渲染。按原始 PRD 规划：Canvas + SVG 混合方案。
- Canvas：连接线（stroke-dasharray 生长动画）
- SVG：交互节点（可访问、可样式化）
- DOM：输入覆盖层、书签侧边栏

---

## 7. 已完成 vs 待规划

### 7.1 已实现

- [x] 含完整设计系统的落地页（index.html）
- [x] 交错菜单组件（index.html + menu-vertical.tsx）
- [x] 三语言 i18n 框架
- [x] 下至 375px 的响应式布局
- [x] 偏好减少动效支持
- [x] 磁性排版、弹性入场、波纹效果
- [x] GSAP 滚动动画和统计计数
- [x] Next.js 项目骨架

### 7.2 进行中 / 待开发

- [ ] 探索画布核心循环（词 → 5 个联想 → 点击 → 重复）
- [ ] 画布上的节点与连线渲染
- [ ] 画布平移/缩放
- [ ] 书签系统界面
- [ ] 已收藏节点的笔记功能
- [ ] 词库 JSON 数据文件
- [ ] 轨迹足迹保留
- [ ] 画布键盘导航
- [ ] 画布节点的屏幕阅读器支持

### 7.3 明确不在 V1 范围内

- [ ] 用户账户/云端存储
- [ ] AI/LLM 生成的联想
- [ ] 浅色主题变体
- [ ] 导出（图片/JSON/Markdown）
- [ ] 协作/分享
- [ ] WebGL 粒子效果
- [ ] 移动端原生应用

---

## 8. 约束条件

1. **离线优先。** 词库必须在无网络环境下工作。核心词汇查找不调用 API。
2. **无需注册。** 探索画布必须在页面加载后即可立即使用。
3. **仅深色主题。** 单一主题，无浅色模式切换。
4. **中文优先词库。** 主词汇数据库为中文，英文为辅助。
5. **零依赖词语引擎。** 联想查询为纯 JS 实现，不依赖外部服务。
